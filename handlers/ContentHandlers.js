export const handleContentClick = (props) => {
  const {
    trackButtonClick,
    setShowHelpModal,
    addMessage,
    setIsTyping,
    config
  } = props;

  return (e) => {
    // Toda a lógica atual do handleContentClick
  };
}; 